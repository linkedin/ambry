package com.github.ambry.rest;

import com.codahale.metrics.MetricRegistry;


/**
 * TODO: write description
 */
public class MockExternalServerMetrics extends ServerMetrics {

  protected MockExternalServerMetrics(MetricRegistry metricRegistry) {
    super(metricRegistry);
  }
}
